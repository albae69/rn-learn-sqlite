import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Splash = () => {
  return (
    <View>
      <Text>this is splash scren...</Text>
    </View>
  );
};

export default Splash;

const styles = StyleSheet.create({});
